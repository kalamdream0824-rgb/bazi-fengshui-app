# mock：完全离线运行；http：调用独立后端
VITE_API_MODE=mock

# 浏览器请求的 API 根地址。开发时可留空，使用 Vite /api 代理。
# 独立部署前端时改成后端公开地址，例如：
# VITE_API_BASE_URL=https://api.example.com/api
VITE_API_BASE_URL=

# 仅影响 `npm run dev` 的代理目标，不影响生产构建。
VITE_API_TARGET=http://localhost:8080
