# 八字排盘 App · 前端技术设计（v0.13 · 精简版）

| 项目 | 内容 |
|---|---|
| 版本 | v0.22（四柱表补齐问真口径·自坐已交付） |
| 日期 | 2026-08-09 |
| 索引 | 见 `docs/design/README.md`；后端见 `backend-design.md` |

## 1. 当前状态

- 8 模块全可走通；测试 95/95、lint 0 问题、构建绿、CI 已配置、已上 GitHub。
- 已完成里程碑：M0.5/M0.8（全模块）、表单组件（时间滚轮/省市下拉）、真太阳时、A2 神煞、A1 命书 PDF、A3 合婚规则、A4a 本地历史+备份、B0（共享组件/懒加载/CI）、C1（大运流年神煞/时辰边界提示）、C2（真实黄历/运势文案）、ESLint+Prettier、会员权益+兑换码、**会员购买（模拟支付）**。
- ✅ **v0.20 新人解读版（方案 B）已交付**——`lib/explainer` 生成"读懂这张盘"（总览 + 六块：日主五行/四柱定位/十神/神煞/大运/流年）；基本排盘页顶部解读卡、专业细盘各页签单块解读；四柱表词库扩展至 15+ 词条（天干/地支/五行/日主/十神全称等）；流年页替换示例占位为真实数据解读；合规测试守护温和参考口径。
- ✅ **v0.21 四柱表补齐问真口径已交付**——新增**副星**行（藏干对应日主的十神，lunar-javascript `getXxxShiShenZhi` 直接提供）、**空亡**行（每柱旬空，日柱旬空命中的落空支高亮）、胎元/命宫/身宫信息条（含纳音）；命书 PDF 同步新增副星/空亡行与胎元命宫身宫；词库补副星/空亡/胎元/命宫/身宫释义；契约与 fixtures 同步扩展，前端 102/102、后端 21/21。
- ✅ **v0.22 自坐行已交付**——新增**自坐**行（本柱天干为太极点、在本柱地支的十二长生，与星运的日干太极点口径区分）；口径复用 lunar 库 `CHANG_SHENG_OFFSET` 同款表（阳干顺行/阴干逆行），契约/fixtures 同步，前端 108/108、后端 21/21。
- 详细历史见 git log，不在本文档堆叠。

## 2. 架构

- `apps/web` 独立可跑；排盘数据层 `BaziApi` 接口（Mock 本地真算 / Http 后端），切换仅改 `VITE_API_MODE`。
- 与后端唯一共享层是 `contracts/`（openapi + fixtures）；互不 import 源码。

## 3. 技术栈（实际）

Vite 8 + React 19 + TS 6.0.3（strict）+ react-router 7 + TanStack Query + zustand + Vitest + ESLint/Prettier + lunar-javascript。

## 4. 路由

`/` 首页 · `/input` 排盘输入 · `/history` 历史 · `/chart` 基本排盘 · `/chart/pro` 专业细盘 · `/comp` 合婚 · `/daily` 每日运势 · `/report` 命书 · `/profile` 我的 · `/membership` 会员中心；底部导航（首页/排盘/我的）在主页面与「我的」二级页（会员中心/历史/命书）保留，登录页隐藏。

## 5. 领域逻辑（lib/，均有测试）

| 模块 | 职责 |
|---|---|
| `baziMapper` | lunar → `PaipanResult`（含真太阳时/大运/流年神煞） |
| `compRules` | 合婚参考规则（生肖六合/纳音/夫妻宫/年柱/缺补） |
| `shenSha` | 四柱 + 大运流年神煞（口径见文档历史，来源公示） |
| `trueSolarTime` | 真太阳时（356 城经度 + NOAA 均时差） |
| `almanac` / `dailyFortune` | 真实黄历 + 温和参考型运势文案 |
| `explainer` | **新人解读引擎（进行中）**：PaipanResult → 人话总览 + 分块要点 + 术语词条 |
| `datePicker` / `wuxing` / `reportPdf` | 工具与命书 PDF |

## 5.5 新人解读引擎（方案 B · 已交付）

### 5.5.1 目标与边界

- 目标：让**没接触过命理的新人**看完排盘结果、专业细盘后，能大致明白「这张盘在说什么」，而不是面对四柱表/十神/神煞发懵。
- 边界（合规红线，必须遵守）：只做**结构解释与性格倾向参考**，不做结论性预测；不出现"你今年会发财/会离婚/何时结婚"类断言；每段保留"传统命理框架下的文化参考"语气。
- 范围：基本排盘页（/chart）+ 专业细盘页（/chart/pro）；合婚/命书/运势暂不纳入（避免结论性内容合规风险）。

### 5.5.2 输出结构

`lib/explainer.ts`：输入 `PaipanResult`（+ gender），输出：

```ts
interface ExplainPoint { label: string; text: string }
interface ExplainBlock {
  title: string          // 如「日主与五行」
  points: ExplainPoint[] // 2-4 条要点，每条 = 术语 + 一句人话
}
interface ChartExplanation {
  overview: string[]     // 2-3 句总览（页面顶部）
  blocks: ExplainBlock[] // 分块解读（可折叠）
}
```

### 5.5.3 解读内容清单

| 块 | 数据来源 | 人话要点 |
|---|---|---|
| 日主与五行 | 日柱天干、`wuXing` | 日主是什么（如"壬水"→江河之水，传统多与流动/灵活相连）；五行旺缺对应的倾向参考 |
| 四柱定位 | `pillars` | 年柱（祖上/早年）、月柱（成长环境）、日柱（自身/婚姻）、时柱（晚年/子女）在传统框架里的定位，一句带过 |
| 十神性格 | `pillars.*.shiShen` | 日主十神（正财/偏印等）的通俗解释；四柱十神整体一句概括 |
| 神煞参考 | `shenSha` | 命局出现的神煞每个一句通俗说法（如天乙贵人：传统称遇事多助力），强调参考 |
| 当前大运 | `daYun[isCurrent]` | 当前处于哪个十年运、传统上该运侧重什么（参考） |
| 流年 | `currentLiuNian` | 今年干支、传统框架下通常怎么理解（参考，不预测具体事件） |

### 5.5.4 术语词库（BaziTable TIPS 扩展）

现有词条仅 5 条（主星/藏干/纳音/星运/神煞），扩展为：

- 基础：天干、地支、五行、日主（出生日的天干代表自己）；
- 十神全称 10 条：正官/七杀/正印/偏印/比肩/劫财/食神/伤官/正财/偏财，各一句通俗解释；
- 结构：大运、流年、十二长生（星运）、藏干；
- 已有 5 条保留并润色。

### 5.5.5 文案样例（1995-10-08 14:30 男命：乙亥 乙酉 壬申 丁未）

> **总览**：你的日主是壬水——命理传统上壬水与江河、流动、开阔相连，多与灵活和包容的性格倾向相关。五行中金偏旺、火偏弱，通常被解读为思辨力强、行动上偏稳健的组合。以下内容均为传统命理框架下的文化参考，不作人生断言。
>
> **十神性格**：日柱坐"正财"，传统解读偏重视实际、重积累的倾向；年柱"伤官"多与表达欲、创造力相关。这些只是倾向参考，不构成对你的评价。

### 5.5.6 合规模板与禁用

- 每段解读开头或结尾带"传统命理框架下……仅作文化参考"；
- 禁用词：一定、必定、注定、今年会发财、会离婚、能活到、克夫/克妻 等结论性断言；
- 不做医疗/投资/婚姻等确定性建议；
- 无命盘（未排盘）时不生成解读。

### 5.5.7 测试计划

- `explainer.test.ts`：固定 fixtures 输入 → 断言总览含日主五行、五行旺缺文本；神煞/十神词条覆盖；含"参考"合规词；不含禁用断言词。
- 组件测试：`/chart` 渲染解读卡、`/chart/pro` 各页签渲染对应解读块；释义点击可弹词条。

## 6. 设计系统

令牌 `styles/tokens.css` + 全局 `app.css` + 共享组件（Button/Card/SegControl/Switch/TabBar/TopBar/BaziTable/WuxingBar/DaYunList/Sheet/Toast/FooterNote/EmptyState/RecordRow/RegionSelect/DateTimePicker）。偏差记录：样式为全局文件（CSS Modules 暂缓，需浏览器验证环境）。

## 7. 待办

- **新人解读内容深度扩展（P0-P2 待办）**：把 `lib/explainer` 从"词典式"升级为"组合型"——**词库（原子词条，百条级）+ 规则（模式/句型，不枚举组合文案）两层架构**，避免组合爆炸；规则带权重只展示 2-3 条、禁词测试随规则扩展、文件分 dictionary/rules/explainer。P0：日主旺衰粗判（得令/得地/得势）、副星/自坐进解读、大运十神；P1：十神组合检测（伤官配印等）、神煞落宫；P2：胎元/命宫/身宫解读、流年冲合（先验证 lunar API）；P3：总览"一句话档案"、词库扩充。位置维持前端（迁移信号：出现第二消费端/运营后台/统一内容出口）。
- 占位功能：关于与合规声明、设置-流派。
- 会员购买：http 联调模式已接入模拟支付（套餐选择 → 创建订单 → 模拟支付回调 → 即时开通/续费）；**真实支付通道待企业资质**，前端无需改动。
- **合婚邀请分享（P2，需后端配对机制）**：生成邀请卡片/链接，对方填入生辰后自动配对出合盘——这是命理 App 的真实社交分享场景。
- 优化项：CSS 作用域化、UI 测试补强、覆盖率统计。

## 8. 与后端边界

- `HttpBaziApi` 已占位，后端上线后切 `VITE_API_MODE=http`，页面零改动。
- 会员/订单：`membershipApi`（getMe/redeem）+ `payApi`（createOrder/mockPay），http 模式走 `/api/v1/orders` 与 `/api/v1/pay/mock-success/{orderId}`。
- 神煞/合婚/运势规则**前端保留**，后端第一版不重复实现；一致性靠 fixtures 回归。
- 账号：http 模式走 `/api/v1/auth`（登录/注册），登录后本地历史自动同步云端；Mock 模式模拟登录。

## 9. 合规红线

- 文案"温和参考"、不绝对预测、保留免责声明；无命盘时不生成个性化运势。

## 10. 变更日志

- v0.22（2026-08-15）：自坐行交付——本柱天干在本柱地支的十二长生（与星运区分太极点）；复用 lunar 库 CHANG_SHENG_OFFSET 同款口径，`ziZuoOf` 计算；契约/fixtures 加 ziZuo；命书 PDF 与词库同步；前端 108/108、lint 0、build 绿。
- v0.21（2026-08-15）：四柱表补齐问真口径交付——副星（藏干十神）/空亡独立行/胎元命宫身宫信息条（lunar-javascript `getXxxShiShenZhi`、`getTaiYuan`、`getMingGong`、`getShenGong` 直接提供，无需自算）；契约与 fixtures 扩展（含 hideGan[].shiShen）；命书 PDF 与词库同步；前端 102/102、lint 0、build 绿。
- v0.20（2026-08-11）：新人解读版（方案 B）交付——`lib/explainer`（总览 + 六块解读 + 术语词库 15+），基本排盘/专业细盘接入，流年示例占位替换为真实数据解读；测试 95/95。
- v0.19.2（2026-08-11）：底部导航在「我的」二级页保留——/membership、/history、/report 显示底部页签（登录页仍隐藏），新增 App 级路由测试。测试 86/86。
- v0.19.1（2026-08-11）：统一登录态失效处理——新增 `services/http.ts` 的 `authFetch`（注入 token；401 清本地登录态并提示"登录已过期，请重新登录"），payApi / membershipApi / httpBaziApi 统一接入；会员购买遇到无效 token（如 mock 残留 token 切 http 模式）不再透出原始错误。测试 84/84。
- v0.19（2026-08-11）：会员中心独立页交付——「我的-会员状态」卡收敛为状态摘要 + 会员中心入口（任何登录态可见），新增 `/membership`（套餐选择/购买续费/兑换码，mock 模式明确提示演示、http 模式走模拟支付）。测试 83/83。
- v0.18.1（2026-08-11）：修复会员状态卡登录状态同步——会员卡改为由 `token` 驱动（已登录即显示会员状态；mock 模式显示"未开通会员 · 演示模式"，http 模式显示购买/兑换；未登录才显示登录引导）。测试 80/80。
- v0.18（2026-08-11）：会员购买接入——"我的-会员状态"卡升级为套餐选择（30 天 ¥29.9 / 90 天 ¥68）+ 购买/续费按钮，`payApi`（createOrder/mockPay），支付成功即时刷新会员卡（模拟支付，真实通道待资质；79/79 测试）。
- v0.14（2026-08-09）：接入账号与云同步（登录/注册页、我的页登录态、历史云端读写），退出登录完成。
- v0.15（2026-08-10）：择日模块交付——事项匹配规则引擎（`lib/dayPicker`，真实黄历宜忌/值星/冲煞），页面 `/day-picker`（70/70 测试）。
- v0.16（2026-08-10）：分享（方案 A）交付——1080 分享卡片图（`lib/share`，html2canvas）+ Web Share API + 复制兜底，接入排盘结果页与每日运势页（72/72 测试）；链接分享页归后端 P2。
- v0.16.1（2026-08-10）：分享改为**图片预览面板**（ShareSheet）——点击分享先展示生成的卡片图，支持 保存图片 / 复制文案 / 系统分享图片文件，避免"只复制到文字"的困惑（73/73 测试）。
- v0.16.2（2026-08-10）：分享场景复盘——**移除每日运势分享**（伪需求）；排盘分享重新定位为"**保存命盘 / 求解读**"；合婚邀请分享列入 P2 待办。
- v0.16.3（2026-08-10）：修复"我的-我的命盘"刷新后无数据——改为"当前结果 → 最新历史记录"兜底（75/75 测试）。
- v0.16.4（2026-08-10）：统一"刷新后回退历史"——新增 `useBaziWithFallback`，排盘页/专业细盘/合婚/命书/运势全部改为当前结果优先、最新历史兜底（77/77 测试）。
- v0.17（2026-08-10）：会员权益前端接入——"我的-会员状态"卡（http 联调模式显示会员/到期时间 + 兑换码输入），`membershipApi`（getMe/redeem）。
- v0.13（2026-08-09）：精简整理（当前状态/决策/待办），历史归档至 git log。
